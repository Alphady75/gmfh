<form method="post" class="d-inline" action="{{ path('admin_signaler_delete', {'id': signaler.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signaler.id) }}">
    <button class="btn btn-sm"><i class="fa fa-trash-alt"></i></button>
</form>
