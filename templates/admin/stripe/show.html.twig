{% extends 'base.html.twig' %}

{% block title %}Stripe{% endblock %}

{% block body %}
    <h1>Stripe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stripe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stripe.name }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ stripe.tarif }}</td>
            </tr>
            <tr>
                <th>StripeKey</th>
                <td>{{ stripe.stripeKey }}</td>
            </tr>
            <tr>
                <th>HexaColor</th>
                <td>{{ stripe.hexaColor }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ stripe.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_stripe_index') }}">back to list</a>

    <a href="{{ path('admin_stripe_edit', {'id': stripe.id}) }}">edit</a>

    {{ include('admin/stripe/_delete_form.html.twig') }}
{% endblock %}
