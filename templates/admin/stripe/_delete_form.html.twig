<form method="post" class="d-inline" action="{{ path('admin_stripe_delete', {'id': stripe.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stripe.id) }}">
    <button class="btn btn-sm text-primary"><i class="fa fa-trash-alt"></i></button>
</form>
