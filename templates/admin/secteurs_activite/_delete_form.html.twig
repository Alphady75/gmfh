<form method="post" action="{{ path('admin_secteurs_activite_delete', {'id': secteurs_activite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secteurs_activite.id) }}">
    <button class="btn">Delete</button>
</form>
