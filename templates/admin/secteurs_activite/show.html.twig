{% extends 'espace.html.twig' %}

{% block title %}SecteursActivite{% endblock %}

{% block body %}
    <h1>SecteursActivite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ secteurs_activite.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ secteurs_activite.name }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ secteurs_activite.created ? secteurs_activite.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ secteurs_activite.updated ? secteurs_activite.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_secteurs_activite_index') }}">back to list</a>

    <a href="{{ path('admin_secteurs_activite_edit', {'id': secteurs_activite.id}) }}">edit</a>

    {{ include('admin/secteurs_activite/_delete_form.html.twig') }}
{% endblock %}
