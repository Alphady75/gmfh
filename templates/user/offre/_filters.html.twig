{{ form_start(form, {attr: {class: 'my-4'}}) }}

<div class="twm-bnr-search-bar mb-2">
  <div class="row">
    <!--Title-->
    <div class="form-group col-7 col-sm-9">
      <label>Quoi</label>
      {{ form_widget(form.query) }}
    </div>

    <!--Find job btn-->
    <div class="form-group col-5 col-sm-3 text-end">
      <button type="submit" class="custom-button-light">
        <span class="btnFetch d-block w-100 text-center">
          {{ button_label|default('Trier') }}&nbsp;
          <i class="fa fa-filter"></i>
        </span>
      </button>
    </div>

  </div>
</div>

<div class="bg-white p-2 radius">
  <div class="radius site-bg-white filter-form mb-2">
    <div class="dropdown">
      <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown"
        aria-expanded="false">
        Secteurs d'activité
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
        {{ form_widget(form.secteur) }}
      </div>
    </div>
    <div class="dropdown">
      <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown"
        aria-expanded="false">
        Statut
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
        {{ form_widget(form.status) }}
      </div>
    </div>
    <div class="dropdown filter-form-select">
      {{ form_widget(form.limit) }}
    </div>
  </div>
  
  <div class="product-filter-wrap d-flex justify-content-between align-items-end">
    <span class="woocommerce-result-count-left">
      Affichage de {{ offres|length }} offre{{ offres|length > 1 ? 's' : '' }} d'emploi{{ offres|length > 1 ? 's' : '' }}
    </span>
  </div>
</div>

{{ form_end(form) }}
