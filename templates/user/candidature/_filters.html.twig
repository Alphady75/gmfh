{{ form_start(form, {attr: {class: 'm-b30'}}) }}

<div class="twm-bnr-search-bar mb-2">
  <div class="row">
    <!--Title-->
    <div class="form-group col-sm-9 col-7">
      <label>Quoi</label>
      {{ form_widget(form.query) }}
    </div>

    <!--Find job btn-->
    <div class="form-group col-sm-3 col-5 text-end">
      <button type="submit" class="custom-button-light">
        <span class="btnFetch d-block w-100 text-center">
          {{ button_label|default('Trier') }}&nbsp;
          <i class="fa fa-filter"></i>
        </span>
      </button>
    </div>

  </div>
</div>

<div class="bg-white p-2 radius">
  <div class="radius site-bg-white filter-form mb-2">
    <div class="dropdown">
      <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown"
        aria-expanded="false">
        Secteurs d'activité
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
        {{ form_widget(form.secteur) }}
      </div>
    </div>
    <div class="dropdown">
      <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown"
        aria-expanded="false">
        Statut de la candidature
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
        {{ form_widget(form.status) }}
      </div>
    </div>
    <div class="dropdown filter-form-select">
      {{ form_widget(form.limit) }}
    </div>
		{% if clear == true %}
		<div class="dropdown">
			<a href="{{ path(path) }}" class="btn text-danger"><span class="btnFetch"><i class="feather-trash me-0"></i></span></a>
		</div>
		{% endif %}
  </div>
  
  <div class="product-filter-wrap d-flex justify-content-between align-items-end pb-0">
    <span class="woocommerce-result-count-left">
      Affichage de {{ candidatures|length }} candidature{{ candidatures|length > 1 ? 's' : '' }}
    </span>
  </div>
</div>

{{ form_end(form) }}

