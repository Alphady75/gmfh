<form method="post" action="{{ path('user_candidature_delete', {'id': candidature.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidature.id) }}">
    <button class="btn">Delete</button>
</form>
