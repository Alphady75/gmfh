{% extends 'espace.html.twig' %}

{% block title %}Mes alertes emplois ({{ alerts.getTotalItemCount }})
{% endblock %}
{% block titre %}Mes alertes emplois ({{ alerts.getTotalItemCount }})
{% endblock %}
{% block text %}Mes alertes emplois ({{ alerts.getTotalItemCount }})
{% endblock %}

{% block right %}
<a href="{{ path('user_alert_new') }}" class="custom-button-light"><i class="fa fa-bell"></i> Créer une
            alerte</a>
{% endblock %}

{% block body %}

<div class="row pb-3">
    <div class="col-sm-6">
        <div class="">
            {{ knp_pagination_render(alerts, null, {}, {
            'align': 'left',
            'size': 'medium',
            'rounded': true,
            }) }}
        </div>
    </div>
</div>


{% include "user/alert/composants/_data.html.twig" %}

<div class="pt-3">
    {{ knp_pagination_render(alerts, null, {}, {
    'align': 'right',
    'size': 'medium',
    'rounded': true,
    }) }}
</div>

{% endblock %}