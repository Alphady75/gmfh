<form method="post" class="d-inline" action="{{ path('client_favoris_delete', {'id': favori.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément en favoris?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favori.id) }}">
    <button class="btn btn-light btn-sm text-primary"><i class="fa fa-trash-alt"></i></button>
</form>
